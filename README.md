# Blog

Installation
  1. Clone the repo
     ```
     git clone 'URL'
     ```

  3. Navigate to the project directory
     cd project_name
     
  4. Install NPM packages
     npm install

  5. Run program
     node index.js
